---
phase: 08
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend/src/pages/contacts/list.vue
  - frontend/src/pages/contacts/detail/detail.vue
  - frontend/src/pages/contacts/edit/edit.vue
  - frontend/src/pages/ocr/cards/cards.vue
  - frontend/src/pages/ocr/result/result.vue
  - frontend/src/pages/interactions/add/add.vue
  - frontend/src/pages/reminders/list/list.vue
  - frontend/src/pages/reminders/add/add.vue
autonomous: true
requirements: [CONTACT-01, CONTACT-02, CONTACT-03, OCR-01, OCR-02, INTERACTION-01, INTERACTION-02, INTERACTION-03, REMINDER-01, CARD-WALL-01]

must_haves:
  truths:
    - "Contact list uses wd-cell/wd-list for items instead of hand-written card items"
    - "Contact list uses wd-search-bar instead of custom search input"
    - "Contact list uses wd-empty when no contacts exist"
    - "Contact edit uses wd-form + wd-input/wd-form-item for fields"
    - "Contact detail uses wd-cell-group for fields"
    - "Reminders list uses wd-cell-group and wd-empty"
    - "Card wall uses wd-empty instead of custom empty state"
  artifacts:
    - path: "frontend/src/pages/contacts/list.vue"
      provides: "Contact list with wd-cell, wd-search-bar, wd-empty"
      contains: "wd-cell\|wd-search-bar\|wd-empty"
    - path: "frontend/src/pages/contacts/edit/edit.vue"
      provides: "Edit form with wd-form, wd-input, wd-form-item"
      contains: "wd-form"
    - path: "frontend/src/pages/contacts/detail/detail.vue"
      provides: "Detail page with wd-cell-group"
      contains: "wd-cell-group"
    - path: "frontend/src/pages/reminders/list/list.vue"
      provides: "Reminders with wd-cell-group and wd-empty"
      contains: "wd-empty"
    - path: "frontend/src/pages/ocr/cards/cards.vue"
      provides: "Card wall with wd-empty"
      contains: "wd-empty"
  key_links:
    - from: "frontend/src/pages/contacts/list.vue"
      to: "wot-design-uni"
      via: "wd-search-bar @search -> loadContacts"
      pattern: "wd-search-bar"
    - from: "frontend/src/pages/contacts/edit/edit.vue"
      to: "wot-design-uni"
      via: "wd-form submit -> handleSave"
      pattern: "wd-form"
---

<objective>
Migrate existing pages from hand-written CSS to wot-design-uni components.

Purpose: Replace all hand-rolled UI elements with standardized wot components for consistent WeChat-native look and feel.
Output: Updated page .vue files with wot component usage, reduced custom CSS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-前端完善与测试/08-CONTEXT.md
@.planning/phases/08-前端完善与测试/08-RESEARCH.md
@.planning/phases/08-前端完善与测试/08-01-PLAN.md
@.planning/phases/08-前端完善与测试/08-02-PLAN.md

<interfaces>
From frontend/src/api/contacts.ts:
```typescript
export interface Contact {
  id: string
  name: string
  company: string | null
  title: string | null
  phone: string | null
  email: string | null
  wechatId: string | null
  avatar: string | null
  tags: string[]
  createdAt: string
}

export function getContacts(params?: Record<string, string>): Promise<ApiResponse<Contact[]>>
export function getContact(id: string): Promise<ApiResponse<ContactDetail>>
export function createContact(data: CreateContactInput): Promise<ApiResponse<{ id: string }>>
export function updateContact(id: string, data: UpdateContactInput): Promise<ApiResponse<void>>
export function deleteContact(id: string): Promise<ApiResponse<void>>
```

From frontend/src/api/reminders.ts:
```typescript
export interface Reminder {
  id: string
  type: 'relationship' | 'birthday' | 'custom'
  message: string
  scheduledAt: string
  sentAt: string | null
  contact?: { id: string; name: string }
}

export function getReminders(): Promise<ApiResponse<Reminder[]>>
export function createReminder(data: CreateReminderInput): Promise<ApiResponse<{ id: string }>>
```

easycom is configured (Plan 01): "^wd-(.*)": "wot-design-uni/components/wd-$1/wd-$1.vue"
No manual imports of wd-* components needed -- easycom auto-resolves them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate contact list, contact detail, and contact edit pages</name>
  <files>frontend/src/pages/contacts/list.vue, frontend/src/pages/contacts/detail/detail.vue, frontend/src/pages/contacts/edit/edit.vue</files>
  <read_first>
    - frontend/src/pages/contacts/list.vue
    - frontend/src/pages/contacts/detail/detail.vue
    - frontend/src/pages/contacts/edit/edit.vue
  </read_first>
  <action>
    1. Migrate contacts/list.vue -- Replace hand-written search and list:
       - Replace custom &lt;view class="search-bar"&gt;&lt;input&gt; with &lt;wd-search-bar placeholder="搜索姓名或公司" @search="handleSearch" @clear="handleSearch" v-model="searchKeyword" /&gt;
       - Replace custom &lt;view class="tag-filter"&gt; with &lt;wd-tab type="line"&gt; containing "全部" + each tag in allTags as clickable tabs, @click="selectedTag = tag; loadContacts()"
       - Replace hand-written .contact-item cards with wd-cell-group wrapping wd-cell for each contact:
         &lt;wd-cell-group&gt;
           &lt;wd-cell v-for="contact in contacts" :key="contact.id" :title="contact.name" :label="contact.company ? contact.company + ' · ' + (contact.title || '') : '暂无公司信息'" is-link @click="goDetail(contact.id)"&gt;
             &lt;template #icon&gt;
               &lt;view class="avatar-circle"&gt;{{ contact.name.charAt(0) }}&lt;/view&gt;
             &lt;/template&gt;
             &lt;template v-if="contact.tags.length > 0" #right-icon&gt;
               &lt;view class="tags-row"&gt;
                 &lt;text v-for="tag in contact.tags" :key="tag" class="tag-badge"&gt;{{ tag }}&lt;/text&gt;
               &lt;/view&gt;
             &lt;/template&gt;
           &lt;/wd-cell&gt;
         &lt;/wd-cell-group&gt;
       - Replace custom .empty state with &lt;wd-empty description="暂无联系人" /&gt;
       - Replace hand-written buttons with wd-button:
         &lt;wd-button type="primary" block @click="goCreate"&gt;+ 添加联系人&lt;/wd-button&gt;
         &lt;!-- #ifndef H5 --&gt;
         &lt;wd-button block @click="goImport"&gt;从通讯录导入&lt;/wd-button&gt;
         &lt;!-- #endif --&gt;
       - Remove all custom CSS except .avatar-circle (width: 64rpx, height: 64rpx, border-radius: 50%, background: #e8f8ef, color: #07c160, display: flex, align-items: center, justify-content: center, font-size: 28rpx, font-weight: 600) and .tag-badge (padding: 2rpx 8rpx, background: #e8f8ef, color: #07c160, border-radius: 4rpx, font-size: 20rpx, margin-left: 8rpx) and .tags-row (display: flex)
       - Keep all existing script logic unchanged (loadContacts, handleSearch, goDetail, goCreate, goImport)

    2. Migrate contacts/detail/detail.vue -- Replace hand-written cards:
       - Replace .info-card with wd-cell-group containing wd-cell items for name, company, tags
       - Replace .detail-card with wd-cell-group for contact info:
         &lt;wd-cell-group title="联系方式"&gt;
           &lt;wd-cell v-if="contact.phone" title="电话" :value="contact.phone" is-link @click="uni.makePhoneCall({ phoneNumber: contact.phone! })" /&gt;
           &lt;wd-cell v-if="contact.email" title="邮箱" :value="contact.email" /&gt;
           &lt;wd-cell v-if="contact.wechatId" title="微信" :value="contact.wechatId" /&gt;
         &lt;/wd-cell-group&gt;
       - Replace .timeline-card with wd-cell-group for interactions:
         &lt;wd-cell-group title="交互记录"&gt;
           &lt;template #extra&gt;
             &lt;wd-button size="small" type="primary" @click="addInteraction"&gt;+ 添加&lt;/wd-button&gt;
           &lt;/template&gt;
           &lt;wd-cell v-for="item in contact.interactions" :key="item.id" :title="getTypeLabel(item.type)" :label="item.content" :value="formatDate(item.occurredAt)" @longpress="handleInteractionLongPress(item)" /&gt;
           &lt;wd-empty v-if="contact.interactions.length === 0" description="暂无交互记录" /&gt;
         &lt;/wd-cell-group&gt;
       - Replace .actions buttons with wd-button:
         &lt;wd-button type="primary" block @click="goEdit"&gt;编辑联系人&lt;/wd-button&gt;
         &lt;wd-button type="error" block plain @click="deleteContact"&gt;删除联系人&lt;/wd-button&gt;
       - Keep all script logic unchanged (loadContact, goEdit, addInteraction, deleteContact, handleInteractionLongPress, getTypeLabel, formatDate)
       - Remove all hand-written card/timeline CSS. Keep only minimal layout padding: .detail-page { padding: 24rpx; }

    3. Migrate contacts/edit/edit.vue -- Replace hand-written form:
       - Replace &lt;view class="form"&gt; with &lt;wd-form ref="formRef"&gt;&lt;wd-form-item label="姓名" prop="name" required :rules="[{ required: true, message: '请输入姓名' }]"&gt;...
       - Use wd-input for text fields with proper type attributes:
         &lt;wd-input v-model="name" placeholder="请输入姓名" /&gt;
         &lt;wd-input v-model="phone" placeholder="请输入电话号码" type="number" /&gt;
         &lt;wd-input v-model="email" placeholder="请输入邮箱" type="email" /&gt;
         &lt;wd-input v-model="wechatId" placeholder="请输入微信号" /&gt;
       - Keep tag-input component as-is (custom component, not replaced)
       - Replace birthday type toggle with wd-radio-group or keep as wd-button toggle but style with wd-button size="small"
       - Keep picker inputs for date/lunar (native picker is fine)
       - Replace .save-btn with &lt;wd-button type="primary" block :loading="saving" @click="handleSave"&gt;保存&lt;/wd-button&gt;
       - Replace manual validation (if !name.value.trim() showToast) with wd-form validate:
         formRef.value?.validate().then(valid => { if (valid) handleSave() })
         OR keep manual validation as fallback (simpler, works without form refactor)
       - Remove all hand-written form CSS. Keep page padding: .edit-page { padding: 24rpx; padding-bottom: 160rpx; }
  </action>
  <verify>
    <automated>cd frontend && cat src/pages/contacts/list.vue | grep -c "wd-cell" && cat src/pages/contacts/list.vue | grep -c "wd-search-bar" && cat src/pages/contacts/list.vue | grep -c "wd-empty" && cat src/pages/contacts/detail/detail.vue | grep -c "wd-cell-group" && cat src/pages/contacts/edit/edit.vue | grep -c "wd-form" && cat src/pages/contacts/edit/edit.vue | grep -c "wd-input"</automated>
  </verify>
  <done>
    - list.vue uses wd-search-bar (no raw input for search), wd-cell for items, wd-empty for empty state
    - detail.vue uses wd-cell-group for info sections, wd-empty for empty timeline
    - edit.vue uses wd-form with wd-input for text fields, wd-button for save
    - All three pages have significantly reduced custom CSS (only layout padding and avatar/tag badges remain)
    - All script logic preserved unchanged
    - H5 conditional compilation applied to contacts import button in list.vue
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate OCR cards, interactions, and reminders pages</name>
  <files>frontend/src/pages/ocr/cards/cards.vue, frontend/src/pages/ocr/result/result.vue, frontend/src/pages/interactions/add/add.vue, frontend/src/pages/reminders/list/list.vue, frontend/src/pages/reminders/add/add.vue</files>
  <read_first>
    - frontend/src/pages/ocr/cards/cards.vue
    - frontend/src/pages/ocr/result/result.vue
    - frontend/src/pages/interactions/add/add.vue
    - frontend/src/pages/reminders/list/list.vue
    - frontend/src/pages/reminders/add/add.vue
  </read_first>
  <action>
    1. Migrate ocr/cards/cards.vue (card wall):
       - Keep the columns: 2 waterfall layout (CSS property, not a wot component concern)
       - Replace custom .empty state with &lt;wd-empty description="暂无名片" icon="scan" /&gt;
       - Replace .scan-btn with &lt;wd-button type="primary" block @click="goScan"&gt;+ 扫描名片&lt;/wd-button&gt;
       - Add click handler on card-item to navigate to card detail: @click="uni.navigateTo({ url: '/pages/card-detail/card-detail?id=' + card.id })"
       - Remove custom empty/scan button CSS. Keep card-item and cards-grid CSS for waterfall layout
       - Keep all script logic unchanged

    2. Migrate ocr/result/result.vue (OCR result confirmation):
       - Replace hand-written form with wd-form:
         &lt;wd-form ref="formRef"&gt;
           &lt;wd-form-item label="姓名" prop="name"&gt;&lt;wd-input v-model="formData.name" placeholder="姓名" /&gt;&lt;/wd-form-item&gt;
           &lt;wd-form-item label="公司" prop="company"&gt;&lt;wd-input v-model="formData.company" placeholder="公司" /&gt;&lt;/wd-form-item&gt;
           &lt;wd-form-item label="职位" prop="title"&gt;&lt;wd-input v-model="formData.title" placeholder="职位" /&gt;&lt;/wd-form-item&gt;
           &lt;wd-form-item label="电话" prop="phone"&gt;&lt;wd-input v-model="formData.phone" placeholder="电话" type="number" /&gt;&lt;/wd-form-item&gt;
           &lt;wd-form-item label="邮箱" prop="email"&gt;&lt;wd-input v-model="formData.email" placeholder="邮箱" type="email" /&gt;&lt;/wd-form-item&gt;
         &lt;/wd-form&gt;
       - Replace buttons with wd-button:
         &lt;wd-button type="primary" block @click="handleConfirm"&gt;确认保存&lt;/wd-button&gt;
         &lt;wd-button block @click="handleRetake"&gt;重新扫描&lt;/wd-button&gt;
       - Remove hand-written form CSS, keep page padding

    3. Migrate interactions/add/add.vue:
       - Replace type selector (hand-written radio buttons) with wd-radio-group:
         &lt;wd-radio-group v-model="formData.type" shape="button"&gt;
           &lt;wd-radio value="manual_note"&gt;手动笔记&lt;/wd-radio&gt;
           &lt;wd-radio value="call"&gt;通话&lt;/wd-radio&gt;
           &lt;wd-radio value="meeting"&gt;会议&lt;/wd-radio&gt;
         &lt;/wd-radio-group&gt;
       - Replace content textarea with wd-textarea:
         &lt;wd-textarea v-model="formData.content" placeholder="记录交互内容" :maxlength="500" show-word-limit /&gt;
       - Replace date picker with wd-datetime-picker or keep native picker
       - Replace submit button with wd-button type="primary" block
       - Remove hand-written form CSS

    4. Migrate reminders/list/list.vue:
       - Replace .summary section with &lt;wd-notice-bar text="你有 X 条待提醒" /&gt;
       - Replace section titles with &lt;wd-cell-group title="今天" /&gt;, title="本周", title=" upcoming"
       - Replace hand-written .reminder-card with wd-cell inside wd-cell-group:
         &lt;wd-cell :title="reminder.message" :label="getTypeLabel(reminder.type) + ' · ' + formatTime(reminder.scheduledAt)"&gt;
           &lt;template #icon&gt;
             &lt;text&gt;{{ getTypeIcon(reminder.type) }}&lt;/text&gt;
           &lt;/template&gt;
           &lt;template v-if="reminder.contact" #right-icon&gt;
             &lt;text class="contact-label"&gt;{{ reminder.contact.name }}&lt;/text&gt;
           &lt;/template&gt;
         &lt;/wd-cell&gt;
       - Replace .empty state with &lt;wd-empty description="暂无提醒" /&gt;
       - Remove hand-written card CSS, keep page padding

    5. Migrate reminders/add/add.vue:
       - Replace hand-written form with wd-form + wd-input/wd-textarea
       - Replace type selector with wd-radio-group (same pattern as interactions)
       - Replace date/time picker with wd-datetime-picker
       - Replace contact selector with wd-picker
       - Replace submit button with wd-button type="primary" block
       - Remove hand-written form CSS
  </action>
  <verify>
    <automated>cd frontend && cat src/pages/ocr/cards/cards.vue | grep -c "wd-empty" && cat src/pages/ocr/result/result.vue | grep -c "wd-form" && cat src/pages/interactions/add/add.vue | grep -c "wd-radio-group" && cat src/pages/reminders/list/list.vue | grep -c "wd-empty" && cat src/pages/reminders/add/add.vue | grep -c "wd-form"</automated>
  </verify>
  <done>
    - cards.vue uses wd-empty for empty state, wd-button for scan button
    - result.vue uses wd-form with wd-input fields, wd-button for actions
    - interactions/add.vue uses wd-radio-group for type selection, wd-textarea for content
    - reminders/list.vue uses wd-cell-group for grouped reminders, wd-empty for empty state
    - reminders/add.vue uses wd-form with wd-input/wd-textarea, wd-datetime-picker for scheduling
    - All five pages have reduced custom CSS (only minimal layout padding remains)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form inputs -> API | All user input goes through API calls -- existing validation in API layer handles this |
| OCR result edit -> Contact creation | User can edit OCR results before saving -- must validate same as manual contact creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | Form input fields | mitigate | wd-form built-in validation rules for required fields; existing API-level validation provides defense in depth |
| T-08-08 | Information Disclosure | Contact detail phone/email | mitigate | Phone clickable only via uni.makePhoneCall (no text copy exposure); email displayed but not auto-linked to prevent scraping |
| T-08-09 | Spoofing | Card detail navigation | accept | Card IDs are server-generated UUIDs; no client-side ID manipulation can access other users' cards due to backend ownership verification |
</threat_model>

<verification>
- All migrated pages render correctly with wot components
- No hand-written CSS for buttons, forms, empty states, or list items remains
- Avatar circles and tag badges retain custom styling (these are not wot component replacements)
- All existing page functionality preserved (navigation, data loading, form submission)
- H5 conditional compilation applied where needed (contacts import hidden)
</verification>

<success_criteria>
- [ ] contacts/list.vue uses wd-search-bar, wd-cell, wd-empty
- [ ] contacts/detail/detail.vue uses wd-cell-group for all sections
- [ ] contacts/edit/edit.vue uses wd-form, wd-input, wd-button
- [ ] ocr/cards/cards.vue uses wd-empty, wd-button
- [ ] ocr/result/result.vue uses wd-form, wd-input
- [ ] interactions/add/add.vue uses wd-radio-group, wd-textarea
- [ ] reminders/list/list.vue uses wd-cell-group, wd-empty
- [ ] reminders/add/add.vue uses wd-form, wd-datetime-picker
- [ ] No hand-written button/form/list/empty CSS remains in migrated pages
- [ ] All script logic (data loading, navigation, event handlers) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-前端完善与测试/08-03-SUMMARY.md`
</output>
