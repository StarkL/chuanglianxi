---
phase: 05-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/interactions.ts
autonomous: true
requirements:
  - INTERACTION-01
  - INTERACTION-02
  - INTERACTION-03
must_haves:
  truths:
    - "User can edit an existing interaction record (type, content, time, duration)"
    - "User can delete an interaction record with confirmation"
    - "Both operations verify contact ownership before proceeding"
  artifacts:
    - path: "backend/src/routes/interactions.ts"
      provides: "PUT /interactions/:id and DELETE /interactions/:id endpoints"
      exports: ["interactionRoutes (with PUT and DELETE handlers)"]
  key_links:
    - from: "backend/src/routes/interactions.ts"
      to: "prisma.interaction"
      via: "database mutation"
      pattern: "prisma\\.interaction\\.(update|delete)"
    - from: "backend/src/routes/interactions.ts"
      to: "prisma.contact"
      via: "ownership verification"
      pattern: "prisma\\.contact\\.findFirst"
---

<objective>
Add PUT and DELETE endpoints to the existing interactions route file, completing the backend CRUD API for interaction records.

Purpose: The frontend add page and timeline edit/delete features require these endpoints. POST already exists (D-13); PUT and DELETE are the missing half of CRUD.
Output: Two new route handlers in backend/src/routes/interactions.ts, following the existing POST pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-interactions/05-CONTEXT.md
@.planning/phases/05-interactions/05-01-PLAN.md

@backend/src/routes/interactions.ts — existing POST handler, pattern to follow
@backend/src/routes/contacts.ts — ownership verification pattern (prisma.contact.findFirst with userId)
@backend/src/middleware/auth.ts — AuthenticatedRequest type, requireAuth middleware
@backend/prisma/schema.prisma — Interaction model (type, content, duration, occurredAt fields)
@backend/src/routes/index.ts — route registration (already registers interactionRoutes, no changes needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PUT /interactions/:id endpoint with ownership verification and field validation</name>
  <read_first>
    - backend/src/routes/interactions.ts (existing POST pattern)
    - backend/src/routes/contacts.ts (ownership verification pattern)
    - backend/prisma/schema.prisma (Interaction model fields)
  </read_first>
  <files>backend/src/routes/interactions.ts</files>
  <action>
Add PUT endpoint inside the existing interactionRoutes function, following the exact pattern of the POST handler:

1. Define UpdateInteractionBody interface with optional fields: type, content, duration, occurredAt
   - type enum: 'voice_note' | 'chat_export' | 'manual_note' | 'call' | 'meeting'

2. Register route: fastify.put('/interactions/:id', { preHandler: [requireAuth] })

3. Handler logic:
   a. Extract { userId } from AuthenticatedRequest
   b. Extract interactionId from request.params, and update fields from request.body
   c. Ownership check: prisma.interaction.findFirst({ where: { id: interactionId, userId } }) — reject if not found
   d. Build update data object: only include fields that are provided (omit undefined). For occurredAt, convert string to Date if provided.
   e. prisma.interaction.update({ where: { id: interactionId }, data })
   f. Return { success: true, data: updatedInteraction }

4. Error handling: throw Error('交互记录不存在') if ownership check fails

Do NOT add a new route file — extend the existing interactions.ts. Do NOT change the POST handler. Follow the exact return format { success: true, data: ... } used by POST.
  </action>
  <verify>
    <automated>grep -n "fastify.put" backend/src/routes/interactions.ts</automated>
  </verify>
  <done>
PUT /interactions/:id endpoint exists with:
- preHandler: [requireAuth]
- Ownership verification (interaction.userId === authenticated userId)
- Updates only provided fields (partial update)
- Returns { success: true, data: updatedInteraction }
- Throws '交互记录不存在' if not found
</done>
</task>

<task type="auto">
  <name>Task 2: Add DELETE /interactions/:id endpoint with ownership verification</name>
  <read_first>
    - backend/src/routes/interactions.ts (file now contains POST + PUT)
    - backend/src/routes/contacts.ts (deleteContact pattern for reference)
  </read_first>
  <files>backend/src/routes/interactions.ts</files>
  <action>
Add DELETE endpoint inside the existing interactionRoutes function:

1. Register route: fastify.delete('/interactions/:id', { preHandler: [requireAuth] })

2. Handler logic:
   a. Extract { userId } from AuthenticatedRequest
   b. Extract interactionId from request.params
   c. Ownership check: prisma.interaction.findFirst({ where: { id: interactionId, userId } }) — reject if not found
   d. prisma.interaction.delete({ where: { id: interactionId } })
   e. Return { success: true }

3. Error handling: throw Error('交互记录不存在') if ownership check fails

Follow the exact same ownership verification pattern as PUT. The DELETE endpoint does not need a request body schema (no body parameters).
  </action>
  <verify>
    <automated>grep -n "fastify.delete" backend/src/routes/interactions.ts</automated>
  </verify>
  <done>
DELETE /interactions/:id endpoint exists with:
- preHandler: [requireAuth]
- Ownership verification (interaction.userId === authenticated userId)
- Returns { success: true } on deletion
- Throws '交互记录不存在' if not found
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input (type, content, duration, occurredAt) in PUT body |
| API → Database | User-scoped mutation — must verify ownership before update/delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | PUT /interactions/:id | mitigate | Validate type against enum in Fastify schema; ownership check via findFirst(userId) before update |
| T-05-02 | Tampering | DELETE /interactions/:id | mitigate | Ownership check via findFirst(userId) before delete — prevents cross-user deletion |
| T-05-03 | Information Disclosure | Both endpoints | accept | Error messages use generic '交互记录不存在' — does not leak whether record exists for other users |
| T-05-04 | Repudiation | Both endpoints | accept | V1 scope: no audit logging required; auth middleware provides user identity for Prisma operations |
</threat_model>

<verification>
- Both PUT and DELETE endpoints are registered in the interactionRoutes function
- Both endpoints use requireAuth preHandler
- Both endpoints verify ownership before any database mutation
- TypeScript compilation succeeds (tsc --noEmit)
</verification>

<success_criteria>
- PUT /interactions/:id endpoint accepts partial updates (any combination of type, content, duration, occurredAt)
- DELETE /interactions/:id endpoint removes the interaction record
- Both endpoints reject requests where the interaction does not belong to the authenticated user
- No modification to existing POST endpoint behavior
</success_criteria>

<output>
After completion, create `.planning/phases/05-interactions/05-01-SUMMARY.md`
</output>
