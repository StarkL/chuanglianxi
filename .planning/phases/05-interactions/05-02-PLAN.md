---
phase: 05-interactions
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/api/interactions.ts
  - frontend/src/pages/interactions/add/add.vue
  - frontend/src/pages/contacts/detail/detail.vue
  - frontend/src/pages.json
autonomous: true
requirements:
  - INTERACTION-01
  - INTERACTION-02
  - INTERACTION-03
must_haves:
  truths:
    - "User can tap '添加' in contact detail page and reach a dedicated add interaction page"
    - "User can select an interaction type, enter content, pick time, and enter duration on the add page"
    - "User can long-press an interaction in the timeline to see edit/delete options"
    - "Adding/editing/deleting an interaction refreshes the contact detail page data"
  artifacts:
    - path: "frontend/src/api/interactions.ts"
      provides: "API utility functions for interactions (create, update, delete)"
      exports: ["createInteraction", "updateInteraction", "deleteInteraction", "InteractionType"]
    - path: "frontend/src/pages/interactions/add/add.vue"
      provides: "Dedicated add interaction page with type selector, content input, time picker, duration input"
      min_lines: 120
    - path: "frontend/src/pages.json"
      provides: "Page route registration for add page"
      contains: "pages/interactions/add/add"
  key_links:
    - from: "frontend/src/pages/contacts/detail/detail.vue"
      to: "frontend/src/pages/interactions/add/add.vue"
      via: "uni.navigateTo with contactId query param"
      pattern: "navigateTo.*interactions/add"
    - from: "frontend/src/pages/interactions/add/add.vue"
      to: "/api/interactions"
      via: "createInteraction API call"
      pattern: "request.*POST.*interactions"
---

<objective>
Build the frontend add interaction page, interactions API utilities, and integrate edit/delete into the contact detail timeline.

Purpose: Replace the existing uni.showModal-based add interaction (D-16) with a dedicated page (D-01, D-02) featuring type selector (D-04), and add long-press edit/delete menu (D-11) to the timeline.
Output: New add page, interactions API utilities, updated detail page with navigation and long-press actions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-interactions/05-CONTEXT.md
@.planning/phases/05-interactions/05-01-SUMMARY.md (after Plan 01 completes — backend PUT/DELETE endpoints)

@frontend/src/api/contacts.ts — API utility pattern to follow
@frontend/src/utils/request.ts — HTTP request wrapper
@frontend/src/pages/contacts/detail/detail.vue — existing timeline, addInteraction function to replace
@frontend/src/pages.json — page registration
@.planning/phases/02-weixin-auth/02-UI-SPEC.md — design system (colors, spacing, typography)

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/utils/request.ts:
```typescript
export function request<T>(options: RequestOptions): Promise<ApiResponse<T>>
// RequestOptions: { url: string, method?: 'GET' | 'POST' | 'PUT' | 'DELETE', data?: Record<string, unknown> }
// ApiResponse<T>: { success: boolean, data?: T, error?: string }
```

From frontend/src/pages/contacts/detail/detail.vue:
```typescript
interface ContactInteraction {
  id: string
  type: string
  content: string
  duration: number | null
  occurredAt: string
}
```

Backend Interaction types (from schema.prisma):
- type enum: 'voice_note' | 'chat_export' | 'manual_note' | 'call' | 'meeting'
- Fields: id, contactId, userId, type, content, duration (seconds), occurredAt, createdAt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interactions API utility file and register add page in pages.json</name>
  <read_first>
    - frontend/src/api/contacts.ts (pattern to follow)
    - frontend/src/utils/request.ts (request wrapper)
    - frontend/src/pages.json (page registration)
  </read_first>
  <files>
    - frontend/src/api/interactions.ts
    - frontend/src/pages.json
  </files>
  <action>
Create two files in parallel:

A) frontend/src/api/interactions.ts — following the exact pattern of contacts.ts:
   1. Define InteractionType type: 'voice_note' | 'chat_export' | 'manual_note' | 'call' | 'meeting'
   2. Define Interaction interface: { id, contactId, type, content, duration, occurredAt, createdAt }
   3. Define CreateInteractionInput: { contactId, type, content, duration?, occurredAt? }
   4. Define UpdateInteractionInput: { type?, content?, duration?, occurredAt? }
   5. Export createInteraction(data): request via POST /interactions
   6. Export updateInteraction(id, data): request via PUT /interactions/:id
   7. Export deleteInteraction(id): request via DELETE /interactions/:id

B) frontend/src/pages.json — add new page entry to the pages array:
   {
     "path": "pages/interactions/add/add",
     "style": {
       "navigationBarTitleText": "添加交互"
     }
   }
   Insert after the last existing entry (pages/ocr/cards/cards).
  </action>
  <verify>
    <automated>grep -n "createInteraction\|updateInteraction\|deleteInteraction" frontend/src/api/interactions.ts && grep -n "interactions/add" frontend/src/pages.json</automated>
  </verify>
  <done>
- frontend/src/api/interactions.ts exists with exported functions: createInteraction, updateInteraction, deleteInteraction
- frontend/src/pages.json includes pages/interactions/add/add entry with nav title "添加交互"
</done>
</task>

<task type="auto">
  <name>Task 2: Create dedicated add interaction page with type selector, content input, time picker, and duration input</name>
  <read_first>
    - frontend/src/pages/contacts/detail/detail.vue (extract contactId from page params pattern)
    - frontend/src/pages/contacts/edit/edit.vue (form layout pattern reference)
    - frontend/src/api/interactions.ts (newly created API utilities)
    - .planning/phases/02-weixin-auth/02-UI-SPEC.md (design system: #07c160 green, rpx units, spacing)
  </read_first>
  <files>frontend/src/pages/interactions/add/add.vue</files>
  <action>
Create frontend/src/pages/interactions/add/add.vue — a dedicated page for adding interactions.

Page structure:
1. <script setup>:
   - Get contactId from page options (same pattern as detail.vue: getCurrentPages(), $page.options)
   - Reactive state: selectedType (default 'manual_note'), content, occurredAt (default now), duration
   - Type options array: [
       { value: 'manual_note', label: '手动笔记', icon: '📝' },
       { value: 'call', label: '通话', icon: '📞' },
       { value: 'meeting', label: '会议', icon: '🤝' },
       { value: 'voice_note', label: '语音笔记', icon: '🎤' },
       { value: 'chat_export', label: '聊天记录', icon: '💬' }
     ]
   - onTypeSelect(type): set selectedType
   - onSubmit(): validate content not empty, call createInteraction({ contactId, type, content, duration, occurredAt }), show success toast, navigateBack
   - Format occurredAt for display (YYYY-MM-DD HH:mm format)

2. <template> — form layout following WeChat mini-program style:
   - Type selector: horizontal scrollable row of 5 items, each showing icon + label, selected item highlighted with green background (#07c160) and white text
   - Content input: textarea with placeholder "输入交互内容", min-height 200rpx
   - Time picker: input field showing current date/time, tap opens uni-app picker (use picker with type="datetime")
   - Duration input: number input with placeholder "时长（秒，可选）"
   - Submit button: fixed at bottom, WeChat green (#07c160), full width

3. <style> — use rpx units, consistent with existing pages:
   - Page background: #f5f5f5
   - Card background: #fff, border-radius: 16rpx, padding: 32rpx
   - Type selector item: padding 16rpx, border-radius: 12rpx, border: 1px solid #eee
   - Selected type: background #07c160, color #fff, border-color #07c160
   - Form field spacing: margin-bottom 32rpx
   - Button: height 80rpx, border-radius: 12rpx, font-size 28rpx
  </action>
  <verify>
    <automated>grep -n "createInteraction\|selectedType\|type.*selector\|手动笔记\|通话\|会议" frontend/src/pages/interactions/add/add.vue</automated>
  </verify>
  <done>
- Add page exists at frontend/src/pages/interactions/add/add.vue
- Type selector with 5 types (manual_note, call, meeting, voice_note, chat_export) using emoji icons
- Content textarea, datetime picker, duration input all present
- Selected type highlighted with WeChat green (#07c160)
- Submit button calls createInteraction and navigates back on success
</done>
</task>

<task type="auto">
  <name>Task 3: Replace showModal add in detail page with navigation + add long-press edit/delete menu to timeline items</name>
  <read_first>
    - frontend/src/pages/contacts/detail/detail.vue (existing timeline and addInteraction function)
    - frontend/src/api/interactions.ts (API utilities)
  </read_first>
  <files>frontend/src/pages/contacts/detail/detail.vue</files>
  <action>
Modify the existing contact detail page with two changes:

A) Replace the addInteraction() function:
   - Remove the existing uni.showModal-based addInteraction (lines 55-81)
   - Replace with: uni.navigateTo({ url: `/pages/interactions/add/add?id=${contact.value.id}` })
   - The "+ 添加" button in the template already triggers addInteraction, so it now navigates to the dedicated page

B) Add long-press action menu to timeline items:
   - Add @longpress="showInteractionMenu(item)" to each .timeline-item in the template
   - Implement showInteractionMenu(interaction):
     uni.showActionSheet({
       itemList: ['编辑', '删除'],
       success: (res) => {
         if (res.tapIndex === 0) editInteraction(interaction)
         if (res.tapIndex === 1) deleteInteractionConfirm(interaction)
       }
     })
   - Implement editInteraction(interaction): navigate to add page with edit mode
     uni.navigateTo({ url: `/pages/interactions/add/add?id=${contact.value.id}&editId=${interaction.id}&type=${interaction.type}&content=${encodeURIComponent(interaction.content)}` })
     Note: For V1, the add page should check for editId in query params and pre-fill the form. If editId exists, call updateInteraction instead of createInteraction on submit.
   - Implement deleteInteractionConfirm(interaction):
     uni.showModal({ title: '确认删除？', content: '删除后无法恢复', confirmColor: '#e64340' })
     On confirm: call deleteInteraction(interaction.id), show success toast, reload contact data via loadContact()

C) Update loadContact to refresh after add/edit/delete:
   - The existing loadContact already works — it is called after the showModal add. Keep the same pattern: after navigating back from add/edit page, use uni.$on or page lifecycle to trigger refresh
   - Simplest approach: use onShow() lifecycle hook to call loadContact(contact.value.id) if contact.value exists

D) Add onShow lifecycle to the script:
   - Import onShow from 'vue'
   - Add onShow(() => { if (contact.value) loadContact(contact.value.id) })
   - This ensures the timeline refreshes when returning from the add page

Do NOT change the timeline HTML structure or CSS — keep existing display (D-07, D-08). Only modify the add interaction behavior and add long-press menu.
  </action>
  <verify>
    <automated>grep -n "navigateTo.*interactions/add\|longpress\|showActionSheet\|editInteraction\|deleteInteractionConfirm\|onShow" frontend/src/pages/contacts/detail/detail.vue</automated>
  </verify>
  <done>
- addInteraction() navigates to /pages/interactions/add/add instead of showing modal
- Timeline items have @longpress handler that shows edit/delete action sheet
- editInteraction navigates to add page with edit parameters
- deleteInteractionConfirm shows confirmation modal then calls delete API
- onShow lifecycle refreshes contact data when returning from add/edit page
- showModal-based add interaction code removed
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User input (content, type, duration, occurredAt) sent to backend — validated server-side |
| Long-press menu → Delete action | Destructive operation requires confirmation modal (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | Add/Edit form input | mitigate | Backend validates type enum and sanitizes content; frontend provides UX validation only |
| T-05-06 | Spoofing | Long-press delete | mitigate | Confirmation modal (uni.showModal) before delete; backend ownership check is ultimate gate |
| T-05-07 | Elevation of Privilege | Edit other user's interaction | mitigate | Backend ownership verification (Plan 01); frontend cannot bypass server-side check |
</threat_model>

<verification>
- Add page is accessible from contact detail page via "添加" button
- Type selector displays all 5 types with emoji icons, selection is visually clear
- Form submission creates interaction via POST /interactions
- Long-press on timeline item shows action sheet with edit/delete options
- Edit navigates to add page with pre-filled data, calls PUT /interactions/:id
- Delete shows confirmation, calls DELETE /interactions/:id, refreshes timeline
- No console.log statements in production code
- pages.json includes the new page route
</verification>

<success_criteria>
- User can navigate from contact detail to dedicated add interaction page (replaces showModal)
- Add page has type selector (5 types with icons), content textarea, datetime picker, duration input
- User can long-press any timeline item to see edit/delete menu
- Editing pre-fills the form and updates via PUT endpoint
- Deleting requires confirmation and removes via DELETE endpoint
- Timeline refreshes automatically after add/edit/delete operations
- UI uses WeChat design system (#07c160 green, rpx units, system fonts)
</success_criteria>

<output>
After completion, create `.planning/phases/05-interactions/05-02-SUMMARY.md`
</output>
