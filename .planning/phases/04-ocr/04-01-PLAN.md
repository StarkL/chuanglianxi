---
phase: 04-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/ocr.ts
  - backend/src/lib/ai-enrich.ts
  - backend/src/routes/ocr.ts
  - backend/src/config/env.ts
  - backend/tests/ocr.test.ts
autonomous: true
requirements: [OCR-01, OCR-02, OCR-03]
must_haves:
  truths:
    - "Calling POST /ocr/business-card with an image returns parsed OCR data from Baidu"
    - "Calling POST /ocr/business-card returns AI-enriched data with company website and inferred fields"
    - "Free tier users are blocked after 10 OCR calls per day"
  artifacts:
    - path: "backend/src/lib/ocr.ts"
      provides: "Baidu OCR client wrapper with recognizeBusinessCard function"
      exports: ["recognizeBusinessCard", "normalizeBaiduBusinessCardResult"]
    - path: "backend/src/lib/ai-enrich.ts"
      provides: "Qwen AI enrichment service with enrichBusinessCardData function"
      exports: ["enrichBusinessCardData"]
    - path: "backend/src/routes/ocr.ts"
      provides: "OCR route calling Baidu OCR then Qwen AI, saving to DB"
      exports: ["ocrRoutes"]
    - path: "backend/src/config/env.ts"
      provides: "Zod schema with BAIDU_OCR_* and QWEN_* env vars"
      contains: "BAIDU_OCR_APP_ID, QWEN_API_KEY"
    - path: "backend/tests/ocr.test.ts"
      provides: "Integration tests for OCR-01, OCR-02, OCR-03"
      contains: "describe('OCR'"
  key_links:
    - from: "backend/src/routes/ocr.ts"
      to: "backend/src/lib/ocr.ts"
      via: "import { recognizeBusinessCard }"
      pattern: "recognizeBusinessCard\\("
    - from: "backend/src/routes/ocr.ts"
      to: "backend/src/lib/ai-enrich.ts"
      via: "import { enrichBusinessCardData }"
      pattern: "enrichBusinessCardData\\("
    - from: "backend/src/routes/ocr.ts"
      to: "prisma.businessCard"
      via: "database save after OCR + AI enrichment"
      pattern: "prisma\\.businessCard\\.create"
---

<objective>
Implement backend OCR integration: Baidu OCR for business card recognition, Qwen AI for information enrichment, and integration into the existing POST /ocr/business-card route.

Purpose: Replace the mock OCR placeholder in ocr.ts with real Baidu OCR and AI enrichment, enabling OCR-01 (photo business card recognition) and OCR-02 (AI auto-completion).
Output: Two new service modules (ocr.ts, ai-enrich.ts), updated ocr.ts route, env.ts schema additions, and test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ocr/04-CONTEXT.md
@.planning/phases/04-ocr/04-RESEARCH.md
@backend/src/routes/ocr.ts
@backend/src/config/env.ts
@backend/prisma/schema.prisma
@backend/src/lib/auth.test.ts

<interfaces>
From backend/src/routes/ocr.ts (existing):
```typescript
// Route: POST /ocr/business-card (already exists, has subscription limit logic)
// Request body: { imageData: string }
// Response: { success: true, data: { cardId: string, ocr: NormalizedOCRData } }
// Subscription check: already implemented, blocks free users at 10/day

// Route: GET /ocr/usage (already exists)
// Response: { success: true, data: { used: number, limit: number, remaining: number } }

// Route: GET /business-cards (already exists)
// Response: { success: true, data: BusinessCard[] }
```

From backend/prisma/schema.prisma (existing):
```typescript
// BusinessCard model fields:
//   id: string (uuid)
//   userId: string (FK -> User)
//   contactId: string? (FK -> Contact, nullable)
//   imageUrl: string
//   ocrData: Json? (stores normalized OCR + AI data)
//   createdAt: DateTime
```

From backend/src/config/env.ts (existing):
```typescript
// Current env schema has: PORT, NODE_ENV, DATABASE_URL, REDIS_URL, CORS_ORIGIN,
// WECHAT_APP_ID, WECHAT_APP_SECRET, JWT_SECRET
// Must add: BAIDU_OCR_APP_ID, BAIDU_OCR_API_KEY, BAIDU_OCR_SECRET_KEY, QWEN_API_KEY
```

From frontend/src/api/contacts.ts (existing):
```typescript
export interface Contact {
  id: string; name: string; company: string | null; title: string | null;
  phone: string | null; wechatId: string | null; email: string | null;
  avatar: string | null; tags: string[]; createdAt: string; updatedAt: string;
}
export function createContact(data: Partial<Contact>): Promise<ApiResponse<Contact>>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Baidu OCR service (backend/src/lib/ocr.ts)</name>
  <files>backend/src/lib/ocr.ts, backend/src/config/env.ts</files>
  <read_first>
    - backend/src/lib/ocr.ts (create new)
    - backend/src/config/env.ts (for env var patterns)
    - backend/prisma/schema.prisma (BusinessCard model fields)
    - .planning/phases/04-ocr/04-RESEARCH.md (Baidu OCR SDK patterns)
  </read_first>
  <action>
    1. Install dependency: cd backend && npm install baidu-aip-sdk

    2. Create backend/src/lib/ocr.ts with the following:

    a. Import AipOcrClient from 'baidu-aip-sdk'

    b. Initialize client using env vars:
       ```
       const ocrClient = new AipOcrClient(
         env.BAIDU_OCR_APP_ID,
         env.BAIDU_OCR_API_KEY,
         env.BAIDU_OCR_SECRET_KEY
       )
       ```

    c. Export function normalizeBaiduBusinessCardResult(raw: unknown): NormalizedOCRData
       - Input: Baidu OCR businessCard() response (words_result array with key/value pairs)
       - Map known keys: '姓名' -> name, '公司' -> company, '职位' -> title, '电话' -> phone, '邮箱' -> email, '网址' -> website, '地址' -> address
       - Return NormalizedOCRData: { name, company, title, phone, email, website, address, wechatId: null }
       - Handle missing keys with empty string defaults

    d. Export async function recognizeBusinessCard(imageBase64: string): Promise<NormalizedOCRData>
       - Call ocrClient.businessCard(imageBase64, { recognize_granularity: 'big' })
       - Check result.error_code -- throw Error(`Baidu OCR failed: ${result.error_msg}`) if non-zero
       - Call normalizeBaiduBusinessCardResult(result) and return normalized data

    3. Define the NormalizedOCRData interface:
       ```
       export interface NormalizedOCRData {
         name: string;
         company: string;
         title: string;
         phone: string;
         email: string;
         website: string | null;
         address: string | null;
         wechatId: string | null;
       }
       ```

    4. Update backend/src/config/env.ts -- add to the zod schema:
       - BAIDU_OCR_APP_ID: z.string().min(1)
       - BAIDU_OCR_API_KEY: z.string().min(1)
       - BAIDU_OCR_SECRET_KEY: z.string().min(1)

    DO NOT: Use console.log for logging. DO NOT hardcode API keys. DO NOT modify the route yet (that's Task 3).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/lib/ocr.ts src/config/env.ts</automated>
  </verify>
  <done>
    - backend/src/lib/ocr.ts exists with exported recognizeBusinessCard and normalizeBaiduBusinessCardResult functions
    - NormalizedOCRData interface exported with all 8 fields
    - backend/src/config/env.ts includes BAIDU_OCR_APP_ID, BAIDU_OCR_API_KEY, BAIDU_OCR_SECRET_KEY in zod schema
    - baidu-aip-sdk installed in backend package.json
    - No console.log statements, no hardcoded keys
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Qwen AI enrichment service (backend/src/lib/ai-enrich.ts)</name>
  <files>backend/src/lib/ai-enrich.ts, backend/src/config/env.ts</files>
  <read_first>
    - backend/src/lib/ai-enrich.ts (create new)
    - backend/src/config/env.ts (to add QWEN_API_KEY)
    - .planning/phases/04-ocr/04-RESEARCH.md (Qwen OpenAI-compatible endpoint patterns)
  </read_first>
  <action>
    1. Install dependency: cd backend && npm install openai

    2. Create backend/src/lib/ai-enrich.ts with:

    a. Import OpenAI from 'openai'

    b. Initialize client with Qwen's OpenAI-compatible endpoint:
       ```
       const qwen = new OpenAI({
         apiKey: env.QWEN_API_KEY,
         baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
       })
       ```

    c. Export async function enrichBusinessCardData(ocrData: NormalizedOCRData): Promise<Partial<NormalizedOCRData>>
       - Build a system prompt (hardcoded, not template-interpolated):
         "You are a professional business card information completion assistant. Based on the following OCR-extracted data, complete any missing information:
         1. If company website is missing, infer from company name (do NOT fabricate URLs, return null if uncertain)
         2. Infer department from title information
         3. Check and correct possible OCR recognition errors
         4. Only return JSON format, no extra explanation"
       - Call qwen.chat.completions.create() with:
         - model: 'qwen-plus'
         - messages: [{ role: 'system', content: systemPrompt }, { role: 'user', content: JSON.stringify(ocrData) }]
         - temperature: 0.1
         - response_format: { type: 'json_object' }
       - Parse response.choices[0].message.content as JSON
       - If JSON parsing fails, try stripping markdown code block fences with regex: /```json\n?([\s\S]*?)\n?```/
       - If still fails, return empty object {} (graceful degradation to raw OCR data)
       - Return partial enrichment result

    d. Add QWEN_API_KEY: z.string().min(1) to backend/src/config/env.ts zod schema

    DO NOT: Template-interpolate user data into the system prompt. DO NOT use console.log. DO NOT make enrichment blocking -- if AI fails, raw OCR data should still be usable.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/lib/ai-enrich.ts</automated>
  </verify>
  <done>
    - backend/src/lib/ai-enrich.ts exists with exported enrichBusinessCardData function
    - Function calls Qwen via OpenAI SDK with correct baseURL (dashscope.aliyuncs.com/compatible-mode/v1)
    - JSON parsing has fallback for markdown code blocks and parse failures
    - QWEN_API_KEY added to env.ts zod schema
    - openai installed in backend package.json
    - System prompt is hardcoded, not template-interpolated
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate OCR + AI into POST /ocr/business-card route</name>
  <files>backend/src/routes/ocr.ts</files>
  <read_first>
    - backend/src/routes/ocr.ts (existing route to modify)
    - backend/src/lib/ocr.ts (recognizeBusinessCard function)
    - backend/src/lib/ai-enrich.ts (enrichBusinessCardData function)
    - backend/src/lib/prisma.ts (prisma client)
  </read_first>
  <action>
    1. Read the existing backend/src/routes/ocr.ts -- keep the subscription limit check and BusinessCard.save logic intact

    2. Add imports at top:
       - import { recognizeBusinessCard } from '../lib/ocr.js'
       - import { enrichBusinessCardData } from '../lib/ai-enrich.js'

    3. Replace the mock ocrResult placeholder (lines 46-56) with:
       a. Call recognizeBusinessCard(imageData) -- the imageData is base64 from request body
       b. Call enrichBusinessCardData(ocrResult) for AI enrichment
       c. Merge AI enrichment into OCR result using spread: { ...ocrResult, ...enriched }
       d. Use the merged result as the final ocrData

    4. Update the BusinessCard.create call to use the merged ocrData

    5. Keep the existing subscription limit check (D-17 -- already implemented) before the OCR call

    DO NOT: Remove the subscription limit check. DO NOT change the response format. DO NOT add console.log.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/routes/ocr.ts</automated>
  </verify>
  <done>
    - POST /ocr/business-card calls recognizeBusinessCard (not returning mock data)
    - POST /ocr/business-card calls enrichBusinessCardData after OCR
    - Subscription limit check (10/day for free users) still present and runs BEFORE OCR call
    - BusinessCard.save uses merged ocrData from OCR + AI enrichment
    - Response format unchanged: { success: true, data: { cardId, ocr } }
  </done>
</task>

<task type="auto">
  <name>Task 4: Create OCR test suite (backend/tests/ocr.test.ts)</name>
  <files>backend/tests/ocr.test.ts</files>
  <read_first>
    - backend/tests/ocr.test.ts (create new)
    - backend/src/lib/auth.test.ts (existing test patterns)
    - backend/src/test-setup.ts (test setup with dotenv)
    - backend/src/lib/ocr.ts (functions to test)
    - backend/src/lib/ai-enrich.ts (functions to test)
  </read_first>
  <action>
    1. Create backend/tests/ocr.test.ts with Vitest (follow patterns from backend/src/lib/auth.test.ts):

    a. Test normalizeBaiduBusinessCardResult (OCR-01):
       - Mock Baidu response: { words_result: [{ key: '姓名', value: '张三' }, { key: '公司', value: '腾讯' }, ...] }
       - Assert normalized result has name='张三', company='腾讯', phone, email etc.
       - Test with empty words_result -- assert all fields default to empty string/null

    b. Test enrichBusinessCardData (OCR-02):
       - Mock Qwen response with JSON string: '{"company": "腾讯科技", "website": "https://www.qq.com"}'
       - Assert enrichment returns partial data with website filled in
       - Test with malformed JSON response -- assert graceful fallback (empty object)
       - Test with markdown-wrapped JSON (```json ... ```) -- assert regex stripping works

    c. Test subscription limit guard (OCR-03):
       - Use Fastify test instance (inject) to call POST /ocr/business-card as free user with 10 existing cards
       - Assert 429 status with error message '今日OCR次数已用完'
       - Test as free user with < 10 cards -- assert 200 status

    2. Add mock functions for Baidu OCR and Qwen API to avoid real external calls during tests:
       - Use vi.mock() to mock 'baidu-aip-sdk' and 'openai' modules
       - Return controlled mock responses

    DO NOT: Call real Baidu or Qwen APIs in tests. DO NOT skip the subscription limit test.
  </action>
  <verify>
    <automated>cd backend && npm test -- --run tests/ocr.test.ts</automated>
  </verify>
  <done>
    - backend/tests/ocr.test.ts exists with describe blocks for normalizeBaiduBusinessCardResult, enrichBusinessCardData, and subscription limit
    - Tests use vi.mock for external API mocking
    - Tests cover: normal OCR parsing, empty OCR handling, JSON enrichment, malformed JSON fallback, markdown-wrapped JSON handling, subscription blocking at 10/day
    - npm test passes with all OCR tests green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend (image upload) | Untrusted image file crosses here -- must validate MIME type and size |
| Backend -> Baidu OCR API | API key transmitted -- must use HTTPS, never log keys |
| Backend -> Qwen AI API | OCR data sent to third-party -- must not send PII beyond card data |
| Backend -> Database | ocrData JSON stored -- must sanitize before rendering in frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | backend/src/lib/ocr.ts, ai-enrich.ts | mitigate | Store BAIDU_OCR_* and QWEN_API_KEY in env vars only; zod schema validates presence at startup; never return keys in API responses |
| T-04-02 | Tampering | backend/src/routes/ocr.ts (image upload) | mitigate | Validate MIME type is image/* only; enforce 4MB max file size via @fastify/multipart limits config |
| T-04-03 | Denial of Service | backend/src/routes/ocr.ts (image upload) | mitigate | Reject images > 4MB before sending to Baidu OCR; use sizeType: ['compressed'] on frontend uni.chooseImage |
| T-04-04 | Elevation of Privilege | backend/src/routes/ocr.ts (subscription limit) | mitigate | Existing subscription check runs server-side BEFORE OCR call; cannot be bypassed from frontend |
| T-04-05 | Tampering | backend/src/lib/ai-enrich.ts (prompt injection) | mitigate | System prompt is hardcoded string; user OCR data is JSON-serialized, not template-interpolated into prompt |
| T-04-06 | Information Disclosure | frontend card wall display | mitigate | Sanitize OCR output before rendering -- escape HTML entities, no v-html/raw HTML rendering of OCR text |
</threat_model>

<verification>
1. Backend TypeScript compiles without errors: cd backend && npx tsc --noEmit
2. All OCR tests pass: cd backend && npm test -- --run tests/ocr.test.ts
3. Environment schema validates: BAIDU_OCR_APP_ID, BAIDU_OCR_API_KEY, BAIDU_OCR_SECRET_KEY, QWEN_API_KEY are required
4. POST /ocr/business-card route calls real Baidu OCR (no mock data returned)
5. AI enrichment runs after OCR and merges results before saving
6. Free users blocked at 10/day with 429 response
</verification>

<success_criteria>
1. backend/src/lib/ocr.ts exports recognizeBusinessCard and normalizeBaiduBusinessCardResult
2. backend/src/lib/ai-enrich.ts exports enrichBusinessCardData with graceful fallback
3. POST /ocr/business-card returns real OCR data from Baidu, enriched by Qwen AI
4. env.ts requires all 4 new environment variables (3 BAIDU + 1 QWEN)
5. Test suite covers OCR parsing, AI enrichment, and subscription limits
6. All tests pass via cd backend && npm test
</success_criteria>

<output>
After completion, create `.planning/phases/04-ocr/04-01-SUMMARY.md`
</output>
