---
phase: 04-ocr
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/src/routes/upload.ts
  - backend/src/routes/index.ts
  - frontend/src/api/ocr.ts
  - frontend/src/pages/ocr/scan/scan.vue
  - frontend/src/pages/ocr/result/result.vue
  - frontend/src/pages.json
autonomous: true
requirements: [OCR-01]
must_haves:
  truths:
    - "User taps '拍照' button and WeChat camera opens"
    - "Captured image is uploaded and OCR result returned"
    - "OCR result shows in an editable form with all extracted fields"
    - "User edits fields and saves, creating a Contact record"
  artifacts:
    - path: "backend/src/routes/upload.ts"
      provides: "POST /ocr/upload route for multipart image upload"
      exports: ["uploadRoutes"]
    - path: "frontend/src/api/ocr.ts"
      provides: "OCR API client functions for upload and recognize"
      exports: ["scanBusinessCard"]
    - path: "frontend/src/pages/ocr/scan/scan.vue"
      provides: "Photo capture + upload + loading state page"
      min_lines: 50
    - path: "frontend/src/pages/ocr/result/result.vue"
      provides: "Editable form page with OCR pre-filled data"
      min_lines: 80
    - path: "frontend/src/pages.json"
      provides: "Route registration for scan and result pages"
      contains: "pages/ocr/scan/scan, pages/ocr/result/result"
  key_links:
    - from: "frontend/src/pages/ocr/scan/scan.vue"
      to: "frontend/src/api/ocr.ts"
      via: "import { scanBusinessCard }"
      pattern: "scanBusinessCard\\("
    - from: "frontend/src/api/ocr.ts"
      to: "backend POST /ocr/upload"
      via: "uni.uploadFile to /ocr/upload"
      pattern: "uploadFile.*ocr/upload"
    - from: "frontend/src/pages/ocr/result/result.vue"
      to: "frontend/src/api/contacts.ts"
      via: "import { createContact } on save"
      pattern: "createContact\\("
---

<objective>
Implement image upload handling (backend multipart route) and frontend business card scan page (camera capture -> upload -> OCR -> editable result form).

Purpose: Enable the complete user flow from taking a photo to editing and saving OCR results (OCR-01).
Output: Backend upload route, frontend OCR API client, scan page, result page, and pages.json route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ocr/04-CONTEXT.md
@.planning/phases/04-ocr/04-RESEARCH.md
@.planning/phases/02-weixin-auth/02-UI-SPEC.md

<interfaces>
From Plan 04-01 (backend/src/lib/ocr.ts):
```typescript
export interface NormalizedOCRData {
  name: string; company: string; title: string; phone: string;
  email: string; website: string | null; address: string | null;
  wechatId: string | null;
}
```

From frontend/src/utils/request.ts (existing -- CANNOT use for file uploads):
```typescript
export function request<T>(options: RequestOptions): Promise<ApiResponse<T>>
// Uses uni.request -- does NOT support multipart/form-data
```

From frontend/src/utils/auth.ts (existing):
```typescript
export function getToken(): string | null
// Returns token from uni.getStorageSync('token')
```

From frontend/src/api/contacts.ts (existing):
```typescript
export function createContact(data: Partial<Contact>): Promise<ApiResponse<Contact>>
// POST /contacts -- used by result page to save as contact
```

From frontend/src/pages/contacts/edit/edit.vue (existing form pattern to reuse):
```vue
// Form structure: <view class="form"> with <view class="form-group"> per field
// Each group: <text class="label">, <input class="input" v-model="field">
// Save button: <button class="save-btn" :loading="saving">保存</button>
// Styles: .edit-page, .form, .form-group, .label, .input, .save-btn
```

From backend/src/routes/index.ts (existing):
```typescript
export async function registerRoutes(fastify: FastifyInstance) {
  // Registers all route modules with /api prefix
  // Must add: fastify.register(uploadRoutes, { prefix: '/api' })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend image upload route (POST /ocr/upload)</name>
  <files>backend/src/routes/upload.ts, backend/src/routes/index.ts</files>
  <read_first>
    - backend/src/routes/upload.ts (create new)
    - backend/src/routes/index.ts (to register new route)
    - backend/src/config/env.ts (for env patterns)
    - backend/src/middleware/auth.ts (for requireAuth middleware)
    - .planning/phases/04-ocr/04-RESEARCH.md (image upload pattern with @fastify/multipart)
  </read_first>
  <action>
    1. Install dependency: cd backend && npm install @fastify/multipart

    2. Create backend/src/routes/upload.ts:
       a. Export async function uploadRoutes(fastify: FastifyInstance)
       b. Register the multipart plugin inside the function: await fastify.register(import('@fastify/multipart'), { limits: { fileSize: 4 * 1024 * 1024 } }) (4MB limit per Pitfall 3)
       c. Create POST /ocr/upload route with preHandler: [requireAuth]
       d. Extract userId from request as AuthenticatedRequest
       e. Get uploaded file: const file = await request.file()
       f. Validate file: if (!file) return 400 with '未上传图片'; if (!file.mimetype.startsWith('image/')) return 400 with '仅支持图片格式'
       g. Create upload directory: mkdirSync(join(process.cwd(), 'uploads', 'cards'), { recursive: true })
       h. Generate unique filename: `${userId}-${Date.now()}.${ext}` where ext derived from file.mimetype.split('/')[1] or defaults to 'jpg'
       i. Stream file to disk: await pipeline(file.file, createWriteStream(filepath))
       j. Return { success: true, data: { imageUrl: `/api/ocr/uploads/cards/${filename}` } }

    3. Register the route in backend/src/routes/index.ts:
       - Import { uploadRoutes } from './upload.js'
       - Add: fastify.register(uploadRoutes, { prefix: '/api' })
       - Place after the ocrRoutes registration

    DO NOT: Use console.log. DO NOT store images as base64 in database. DO NOT accept non-image MIME types. DO NOT modify backend/src/config/cors.ts (not needed for API routes).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/routes/upload.ts src/routes/index.ts</automated>
  </verify>
  <done>
    - backend/src/routes/upload.ts exists with POST /ocr/upload route
    - Route requires auth via requireAuth middleware
    - @fastify/multipart registered with 4MB fileSize limit
    - File validated for image/* MIME type
    - File saved to uploads/cards/ directory with unique filename
    - Response returns { success: true, data: { imageUrl } }
    - Route registered in backend/src/routes/index.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend OCR API client and scan page</name>
  <files>frontend/src/api/ocr.ts, frontend/src/pages/ocr/scan/scan.vue, frontend/src/pages.json</files>
  <read_first>
    - frontend/src/api/ocr.ts (create new)
    - frontend/src/pages/ocr/scan/scan.vue (create new)
    - frontend/src/pages.json (to register pages)
    - frontend/src/utils/request.ts (to understand why uni.uploadFile is needed)
    - frontend/src/utils/auth.ts (for getToken)
    - .planning/phases/04-ocr/04-RESEARCH.md (uni.uploadFile and scan page patterns)
    - .planning/phases/02-weixin-auth/02-UI-SPEC.md (styling standards)
  </read_first>
  <action>
    1. Create frontend/src/api/ocr.ts:
       a. Define interface OcrResultData matching NormalizedOCRData from backend:
          { name: string; company: string; title: string; phone: string; email: string; website: string | null; address: string | null; wechatId: string | null }
       b. Define interface OcrScanResult: { cardId: string; ocr: OcrResultData }
       c. Export function uploadBusinessCardImage(filePath: string): Promise<string>
          - Uses uni.uploadFile (NOT uni.request -- Pitfall 5) to POST to /ocr/upload
          - Header includes Authorization: Bearer ${getToken()}
          - Returns the imageUrl string from response
       d. Export async function scanBusinessCard(filePath: string): Promise<OcrScanResult>
          - Step 1: Call uploadBusinessCardImage(filePath) to get imageUrl
          - Step 2: Convert file to base64 using uni.getFileSystemManager().readFile({ filePath, encoding: 'base64' })
          - Step 3: Call request() POST /ocr/business-card with { imageData: base64String }
          - Return { cardId, ocr } from response

    2. Create frontend/src/pages/ocr/scan/scan.vue:
       a. Script: scanning ref (boolean), handleTakePhoto function
       b. handleTakePhoto:
          - uni.chooseImage({ count: 1, sizeType: ['compressed'], sourceType: ['camera'] }) per D-06
          - Set scanning = true (loading state per D-07)
          - Call scanBusinessCard(tempFilePath)
          - On success: uni.navigateTo to /pages/ocr/result/result with OCR data as URL params: encodeURIComponent(JSON.stringify(ocrData)) and cardId
          - On error: uni.showToast({ title: '识别失败', icon: 'none' })
          - Set scanning = false in finally block
       c. Template:
          - Camera section with button "拍摄名片"
          - Tip text: "请将名片置于取景框内，保持光线充足"
          - Loading overlay (v-if="scanning") with spinner and "正在识别名片..." text (per D-07)
       d. Styles: Follow UI-SPEC -- #07c160 for primary button, #f5f5f5 page background, #fff card surfaces, rpx units

    3. Register pages in frontend/src/pages.json:
       - Add: { "path": "pages/ocr/scan/scan", "style": { "navigationBarTitleText": "扫描名片" } }
       - Add: { "path": "pages/ocr/result/result", "style": { "navigationBarTitleText": "确认名片信息" } }

    DO NOT: Use uni.request for file upload (must use uni.uploadFile). DO NOT use camera component (use uni.chooseImage per D-06). DO NOT skip compressed sizeType.
  </action>
  <verify>
    <automated>cd frontend && grep -c "uni.uploadFile" src/api/ocr.ts</automated>
    <automated>cd frontend && grep -c "uni.chooseImage" src/pages/ocr/scan/scan.vue</automated>
    <automated>cd frontend && grep "pages/ocr/scan/scan" src/pages.json && grep "pages/ocr/result/result" src/pages.json</automated>
  </verify>
  <done>
    - frontend/src/api/ocr.ts exists with scanBusinessCard function using uni.uploadFile for upload
    - frontend/src/pages/ocr/scan/scan.vue exists with camera button, loading state, and navigation to result page
    - frontend/src/pages.json has both scan and result pages registered
    - scan page uses uni.chooseImage with sizeType: ['compressed'], sourceType: ['camera']
    - Loading overlay shows "正在识别名片..." during scanning
    - Error toast shows "识别失败" on failure
  </done>
</task>

<task type="auto">
  <name>Task 3: Create OCR result page (editable form before saving as contact)</name>
  <files>frontend/src/pages/ocr/result/result.vue</files>
  <read_first>
    - frontend/src/pages/ocr/result/result.vue (create new)
    - frontend/src/pages/contacts/edit/edit.vue (existing form pattern to reuse)
    - frontend/src/api/contacts.ts (createContact function)
    - frontend/src/api/ocr.ts (OcrResultData type)
    - .planning/phases/02-weixin-auth/02-UI-SPEC.md (styling standards)
  </read_first>
  <action>
    1. Create frontend/src/pages/ocr/result/result.vue -- follows the same form pattern as edit/edit.vue:

    a. Script:
       - Import createContact from '../../../api/contacts.js'
       - Import type OcrResultData from '../../../api/ocr.js'
       - Refs: name, company, title, phone, email, wechatId (all string refs, matching edit.vue pattern)
       - cardId ref (string, from URL params)
       - saving ref (boolean)
       - onMounted: parse URL params -- get cardId, decodeURIComponent the 'data' param via JSON.parse for ocrData
       - Pre-fill form fields from OCR data (per D-08: user can manually modify)
       - handleSave function:
         - Validate name is not empty: if (!name.value.trim()) showToast('请输入姓名')
         - Set saving = true
         - Call createContact({ name, company, title, phone, email, wechatId, source: 'ocr' })
         - On success: uni.showToast('已创建'), uni.navigateBack()
         - On error: uni.showToast('保存失败')
         - Set saving = false in finally

    b. Template -- reuse edit.vue form structure (per D-08):
       - <view class="edit-page"> with <view class="form"> container
       - Form groups for: 姓名(required), 公司, 职位, 电话, 邮箱, 微信号
       - Each field pre-filled with OCR data
       - <button class="save-btn" :loading="saving" @click="handleSave">确认保存为联系人</button>

    c. Styles -- copy from edit/edit.vue for consistency:
       - .edit-page, .form, .form-group, .label, .input, .save-btn
       - Use same colors: #07c160 save button, #f5f5f5 background, #fff form surface
       - Button text: "确认保存为联系人" (different from edit.vue "保存" to indicate this creates new contact)

    DO NOT: Auto-create contact without user confirmation (per deferred: no auto-association). DO NOT use v-html for OCR data rendering. DO NOT skip name validation.
  </action>
  <verify>
    <automated>cd frontend && grep -c "createContact" src/pages/ocr/result/result.vue</automated>
    <automated>cd frontend && grep "确认保存为联系人" src/pages/ocr/result/result.vue</automated>
  </verify>
  <done>
    - frontend/src/pages/ocr/result/result.vue exists with editable form pre-filled from OCR data
    - Form has fields: 姓名(required), 公司, 职位, 电话, 邮箱, 微信号
    - Save button calls createContact API with form data and source: 'ocr'
    - Validates name is not empty before saving
    - Reuses edit.vue form styling pattern (same CSS classes)
    - User can modify any field before confirming (per D-08)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend (upload) | Untrusted image file -- validated by MIME type and size in upload route |
| Frontend URL params | OCR data passed via URL params -- parse safely, no eval() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | backend/src/routes/upload.ts | mitigate | Validate file.mimetype starts with 'image/'; reject non-image MIME types; @fastify/multipart limits fileSize to 4MB |
| T-04-08 | Information Disclosure | frontend/src/api/ocr.ts | mitigate | Authorization token sent via header from getToken(); token never logged or stored in URL params |
| T-04-09 | Tampering | frontend URL params (OCR data) | mitigate | OCR data passed via encodeURIComponent/JSON -- parsed with JSON.parse in try/catch, no eval() or Function() calls |
</threat_model>

<verification>
1. Backend upload route accepts multipart image uploads: file saved to uploads/cards/, imageUrl returned
2. Frontend scan page opens camera via uni.chooseImage, shows loading during OCR
3. Result page pre-fills form with OCR data, user can edit, save creates Contact
4. pages.json has both new pages registered
</verification>

<success_criteria>
1. backend/src/routes/upload.ts handles POST /ocr/upload with @fastify/multipart, saves to uploads/cards/
2. frontend/src/api/ocr.ts exports scanBusinessCard using uni.uploadFile (not uni.request)
3. frontend/src/pages/ocr/scan/scan.vue has camera button, loading overlay, navigation to result
4. frontend/src/pages/ocr/result/result.vue has editable form with OCR pre-filled data, saves via createContact
5. Both pages registered in frontend/src/pages.json
6. Form reuses edit.vue styling pattern (same CSS classes, WeChat green theme)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ocr/04-02-SUMMARY.md`
</output>
